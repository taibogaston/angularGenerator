import { Routes } from '@angular/router';
import { HomeComponent } from './home.component';

export const homeRouting: Routes = [
  {
    path: '',
    component: HomeComponent // Muestra el componente principal de "inicio"
  },
];
